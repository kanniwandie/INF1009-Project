#include "DriverlessVehicle.h"

using namespace std;

DriverlessVehicle::DriverlessVehicle(const string& id, const string& destination, const string& scheduledTime) : Entity(id, destination, scheduledTime) {
}